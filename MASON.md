# github.com/provasign/mason — project map for coding agents

_Generated by `mason init` from the tree and the code graph. Edit freely —
mason loads this file (and AGENTS.md) into every session's system prompt._

## Overview

52 files · Go (44) · code graph: 407 symbols, 3374 edges

## Layout

- `internal/` — 39 files
- `cmd/` — 5 files

## Build & test

- `go build ./...`
- `go test ./...`

## Entry points

- `cmd/mason/main.go` — `func main`

## Conventions

- Build with `GOWORK=off go build ./...` when the parent workspace is present.
- Every behavior ships with a test; run `go test ./... -race` before a release.
- Comments state the constraint or the measured failure that motivated the
  code — not what the next line does.
- Session/permission/redaction guarantees are harness properties: never move
  an enforcement point into a prompt.
